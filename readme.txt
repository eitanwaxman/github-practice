This is the first commit

This is a local change 

After pull - trying to push local change again
Still can't

Creating branch with checkout (git checkout -b new-branch)

pushed new branch (git push -u origin new-branch)